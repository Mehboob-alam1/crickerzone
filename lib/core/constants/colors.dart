import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF12100E); // Very dark warm brown
  static const Color surface = Color(0xFF1E1B18); // Dark warm grey
  static const Color primary = Color(0xFFFFA000); // Amber/Orange for high visibility
  static const Color secondary = Color(0xFFFF5722); // Deep Orange
  static const Color accent = Color(0xFFFFD54F); // Warm Yellow
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFECE0D1); // Warm tinted white
  static const Color textMuted = Color(0xFF9E948A); // Warm muted grey
  static const Color error = Color(0xFFFF5252); // Bright Red
  static const Color cardGrey = Color(0xFF2B2825);
}
